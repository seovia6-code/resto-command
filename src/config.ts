// Public, browser-safe config only.
// DASHBOARD_TOKEN is a server-side secret and must NEVER be imported here.
// Read it via `process.env.DASHBOARD_TOKEN` inside server functions / server routes.
export const WORKER_BASE_URL =
  "https://whatsapp-vapi-bridge.amitoffical17.workers.dev";
